\name{pkpd.txback}
\alias{pkpd.txback}
\docType{data}
\title{
pkpd.txback Csnippet
}
\description{
%%  ~~ A concise (1-5 lines) description of the dataset. ~~
}
\usage{data("pkpd.txback")}
\format{
  The format is:
 chr "\n  TCirc0 = exp(Circ0);\n  TkTR = exp(kTR);\n  TEmax = exp(Emax);\n  TEC50 = exp(EC50);\n  TCL = exp(CL);\n  T"| __truncated__
}
\details{
%%  ~~ If necessary, more details than the __description__ above ~~
}
\source{
%%  ~~ reference to a publication or URL from which the data were obtained ~~
}
\references{
%%  ~~ possibly secondary sources and usages ~~
}
\examples{
data(pkpd.txback)
## maybe str(pkpd.txback) ; plot(pkpd.txback) ...
}
\keyword{datasets}
